# Sub-project git repos (each manages their own git history)
# These are separate git repos — clone them independently
joji-charity/
party-invites/
business-agents/
office-skills/
sandbox-systems/
pilot-agents/

# OS
.DS_Store

# Ralph archives (keep local only)
ralph/archive/
ralph/.last-branch

# Node / Python deps (installed locally per project)
node_modules/
office-skills/node_modules/
office-skills/venv/

# Agent OS specs (work in progress, keep local)
agent-os/specs/

# Local settings (machine-specific)
.claude/settings.local.json
.cursor/

# Agent run outputs (local ephemeral data)
outputs/
pilot-agents/outputs/

# Log files
*.log

# Stale DB copy in dashboards/ (real DB is in business-agents/)
dashboards/business_agents.db

# Personal projects
party-invites/
joji-charity/
